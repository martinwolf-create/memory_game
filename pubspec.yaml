name: memory_game
description: Ein interaktives Memory-Spiel mit Sounds, Animation und Firebase-Anbindung.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

  # 💬 State
  provider: ^6.1.2

  # 🎵 Audio
  audioplayers: ^6.1.0

  # 🎨 Animationen
  flutter_animate: ^4.5.0
  lottie: ^3.1.2

  # 🖼️ Bilder / Caching (optional)
  cached_network_image: ^3.3.1

  # 🔗 Links
  url_launcher: ^6.3.0

  # 📱 Responsive (optional)
  flutter_screenutil: ^5.9.3

  # ⚙️ Utilities
  shared_preferences: ^2.3.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    # Bilder/Icons/Hintergründe (wie bei dir)
    - assets/images/
    - assets/icons/
    - assets/background/

    # Sounds: ein Eintrag reicht rekursiv für alle Kategorien
    - assets/sounds/
    # Effekte: Erfolg/Fehler/Click etc.
    - assets/sfx/
